void main() //main function
{
  int? nullableInt; //with ? is nullable type
  print(nullableInt); //prints null as output

  int nonNullableInt = 10; //without ? is non nullable type
  print(nonNullableInt);
}
